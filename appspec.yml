version: 0.0
os: linux
file_exists_behavior: OVERWRITE

files:
  - source: dist/
    destination: /var/www/deviceloop-frontend
    overwrite: true

hooks:
  AfterInstall:
    - location: scripts/reload-nginx.sh
      timeout: 60
      runas: root
